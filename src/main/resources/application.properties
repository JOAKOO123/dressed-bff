# =============================================
# SERVIDOR
# =============================================
server.port=${SERVER_PORT:8080}

# =============================================
# BACKEND URL
# =============================================
backend.url=${BACKEND_URL:http://localhost:8081}

# =============================================
# JWT
# =============================================
jwt.secret=${JWT_SECRET:dev-secret-local-must-be-32-chars-min}
jwt.cookie.name=${JWT_COOKIE_NAME:access_token}
jwt.cookie.max-age=${JWT_COOKIE_MAX_AGE:86400}

# =============================================
# CORS
# =============================================
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}